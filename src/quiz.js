/**
 * Function to update the name of the relevant quiz.
 * 
 * @param {string} userId 
 * @param {string} quizId 
 * @param {string} name 
 * @returns {object}
 */
function adminQuizNameUpdate(userId, quizId, name) {
    return {};
}

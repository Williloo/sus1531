describe('tests for adminQuestionMove', () => {
  describe('success cases', () => {
    test('successfully move questions', () => {

    });

    test('successfully moves questions when there are multiple quizzes and users', () => {

    });
  });

  describe('error cases', () => {
    test('empty session', () => {

    });

    test('invalid session', () => {

    });

    test('quiz doesn\'t exist', () => {

    });

    test('quiz isn\'t owned by user', () => {

    });

    test('questionId doesn\'t exist', () => {

    });

    test('questionId refers to question in another quiz', () => {

    });

    test('new position less than 0', () => {

    });

    test('new position longer than list', () => {

    });

    test('new position is current position', () => {

    });
  });
});